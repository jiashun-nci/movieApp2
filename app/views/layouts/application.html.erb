<!DOCTYPE html>
<html>
  <head>
    <title>MovieApp2</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
<nav class="navbar navbar-expand-lg bg-dark">
  <div class="collapse navbar-collapse" id="navbarTogglerDemo01">
    <ul class="navbar-nav mr-auto mt-2 mt-lg-0 ">
      <li class="nav-item">
        <a class="nav-link text-success" href="/">Home</a>
      </li>
      <li class="nav-item">
        <a class="nav-link text-success" href="#">Contact Us</a>
      </li>
    </ul>
    <!-- Add Login /sign up links -->
    <% if user_signed_in? %>
      <span class="text-white">Welcome! </span><strong class="text-white ml-2"><%= current_user.email %></strong>.
      <%= link_to 'Edit profile', edit_user_registration_path, :class => 'btn btn-outline-success' %> |
      <%= link_to "Logout", destroy_user_session_path, method: :delete, :class => 'btn btn-outline-danger'  %>
    <% else %>
      <%= link_to "Sign up", new_user_registration_path, :class => 'btn btn-outline-info'  %> |
      <%= link_to "Login", new_user_session_path, :class => 'btn btn-outline-success'  %>
    <% end %>
    <!-- END of Login /sign up links -->
  </div>
</nav>

<!--  Add notice and Alert -->
<% if notice %>
  <p class="alert alert-success"><%= notice %></p>
<% end %>
<% if alert %>
  <p class="alert alert-danger"><%= alert %></p>
<% end %>

    <%= yield %>

    <!-- Footer Copyright -->
    <footer  style="position:relative; bottom:0px; width: 100%;">
    <div class="text-center bg-secondary text-white p-3 mt-3 ">
      © <%= Time.now.year %> Copyright.
      <a class="text-white" href="https://rubyonrails.org/">Build with Ruby on Rails</a>
    </div>
    <!-- Copyright -->
  </footer>
  </body>
</html>
